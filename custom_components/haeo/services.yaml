save_diagnostics:
  fields:
    config_entry:
      required: true
      selector:
        config_entry:
          integration: haeo
    time:
      required: false
      selector:
        datetime:

run_optimizer:
  fields:
    config_entry:
      required: true
      selector:
        config_entry:
          integration: haeo
