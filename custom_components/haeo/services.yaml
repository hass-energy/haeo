save_diagnostics:
  fields:
    config_entry:
      required: true
      selector:
        config_entry:
          integration: haeo
    timestamp:
      required: false
      selector:
        datetime:
